# 对话 Chat — 端到端测试用例

> 测试人员视角整理的**应有** E2E 用例清单,作为实现依据。
> 状态说明:✅ 已实现(链接到 spec) / ⬜ 待实现。
> 端点:UI + 内嵌 API 3020 · LLM 对话依赖 OPENAI/ANTHROPIC key
> 说明:Chat 为 NextChat 定制版(UCAN/钱包登录 + Router/WebDAV 代理),前端与 Next.js API 同源部署在 3020;LLM 对话在 `OPENAI_API_KEY` / `ANTHROPIC_API_KEY`(或有效 Router 令牌)均不可用时跳过。
> 说明:一条逻辑用例可能对应多个 spec;"已实现"以逻辑用例是否被现有 spec 覆盖为准,而非 spec 中的 test 块数量。
> 最后更新:2026-09-16

## 覆盖总览

| 模块 | 用例数 | 已实现 | 待实现 |
| --- | --- | --- | --- |
| 一、健康检查与冒烟 | 6 | 3 | 3 |
| 二、鉴权与登录准入 | 9 | 1 | 8 |
| 三、首页导航与路由 | 7 | 1 | 6 |
| 四、新建对话(new-chat) | 7 | 0 | 7 |
| 五、LLM 会话(依赖 key/令牌) | 13 | 0 | 13 |
| 六、会话管理 | 8 | 0 | 8 |
| 七、Provider 代理 API | 8 | 0 | 8 |
| 八、设置(Settings) | 7 | 0 | 7 |
| 九、云同步与 WebDAV 代理 | 6 | 0 | 6 |
| 十、Router 令牌与充值 | 5 | 0 | 5 |
| 十一、技能/面具/插件/工具/发现 | 7 | 0 | 7 |
| 十二、图像生成(Sd) | 4 | 0 | 4 |
| 十三、导出与分享 | 5 | 0 | 5 |
| **合计** | **92** | **5** | **87** |

---

## 一、健康检查与冒烟

### CH-001 首页返回 2xx
- 优先级:P0
- 类型:API
- 状态:✅ 已实现 — products/chat/tests/smoke.spec.ts
- 前置条件:Chat 服务在 3020 可用
- 步骤:
  1. `GET /`
- 预期结果:返回状态码 < 500(SPA 首页可正常响应)

### CH-002 首页渲染出非空正文
- 优先级:P0
- 类型:UI
- 状态:✅ 已实现 — products/chat/tests/smoke.spec.ts;products/chat/tests/user-flow.spec.ts
- 前置条件:前端 3020 可用
- 步骤:
  1. 访问首页
  2. 读取 `body` 文本
- 预期结果:SPA 挂载成功,`body` 文本非空(user-flow 断言长度 > 50),冒烟通过

### CH-003 无 provider key 时 LLM 会话用例跳过
- 优先级:P1
- 类型:E2E
- 状态:✅ 已实现 — products/chat/tests/smoke.spec.ts
- 前置条件:无
- 步骤:
  1. 读取 `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` 是否存在
- 预期结果:两者都为空时依赖 LLM 的会话用例整体 `test.skip`;至少一个存在时该守卫用例通过

### CH-004 存活探针 /health/live 返回 ok
- 优先级:P0
- 类型:API
- 状态:⬜ 待实现
- 前置条件:服务已启动
- 步骤:
  1. `GET /health/live`
- 预期结果:返回 200,JSON `{status:"ok", service:"chat"}`,响应头 `Cache-Control: no-store`

### CH-005 就绪探针 /health/ready 返回版本
- 优先级:P0
- 类型:API
- 状态:⬜ 待实现
- 前置条件:服务已启动
- 步骤:
  1. `GET /health/ready`
- 预期结果:返回 200,JSON `{status:"ok", service:"chat", version:<BUILD_VERSION 或 "unknown">}`

### CH-006 运行时公开配置下发 /api/config
- 优先级:P1
- 类型:API
- 状态:⬜ 待实现
- 前置条件:服务已启动
- 步骤:
  1. `GET /api/config`(POST 同样受理)
- 预期结果:返回 200,JSON 含 `needCode`、`hideUserApiKey`、`disableFastLink`、`defaultModel`、`ucanLoginForceMode`、`routerPortalUrl` 等公开字段;`Cache-Control: no-store`;不泄露任何私有 Key 明文

---

## 二、鉴权与登录准入

### CH-007 登录页呈现登录入口
- 优先级:P0
- 类型:UI
- 状态:✅ 已实现 — products/chat/tests/user-flow.spec.ts
- 前置条件:前端 3020 可用,未登录
- 步骤:
  1. 访问首页
  2. 检查页面中可交互的登录/钱包相关控件
- 预期结果:DOM 中存在至少一个 `input/button/a/[role=button]` 等登录入口(钱包账号输入、登录按钮等)

### CH-008 未登录访问受保护路由重定向到 /auth
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:清空本地授权会话(未登录)
- 步骤:
  1. 直接访问 `/#/chat`(或 `/#/settings`)
  2. 观察路由变化
- 预期结果:被重定向到 `/#/auth?redirect=<原路径>`,仅 `/auth`、`/help`、`/artifacts` 允许免登录访问

### CH-009 登录页展示钱包账号选择与历史
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:进入 `/#/auth`
- 步骤:
  1. 观察钱包账号输入框及历史下拉
- 预期结果:渲染钱包账号输入(`Locale.Auth.Input` 占位)、清除选择按钮;有历史时展示历史账号列表,无历史时显示空态文案

### CH-010 强制钱包模式下未检测到钱包给出提示
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:`UCAN_LOGIN_FORCE_MODE=wallet`,浏览器未注入 YeYing Wallet
- 步骤:
  1. 加载首页触发钱包检测
- 预期结果:弹出"未检测到钱包"错误提示(安装/启用扩展指引),`localStorage.hasConnectedWallet=false`,页面无 JS 崩溃

### CH-011 钱包 SIWE 登录成功后进入应用
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:注入可签名的 EIP-1193 钱包 shim,或预置有效 UCAN 授权会话
- 步骤:
  1. 在 `/#/auth` 选择钱包账号并触发登录/签名
  2. 等待授权完成
- 预期结果:`isValidUcanAuthorization()` 为真,自动跳回 `redirect` 目标(默认首页),侧栏渲染登录后布局

### CH-012 中心化通行证(central UCAN)登录链路
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:配置 `CENTRAL_UCAN_APP_ID`/`CENTRAL_UCAN_REDIRECT_URI`,`UCAN_LOGIN_FORCE_MODE=central` 或 auto
- 步骤:
  1. 在 `/#/auth` 发起通行证授权
  2. 完成回调 `central-ucan-callback`
- 预期结果:授权请求创建成功并回调换取会话;成功后提示 `钱包身份登录成功`,`getCentralUcanExpiresAt()` 返回有效期

### CH-013 钱包账户与应用账户不一致时给出选择
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:应用已绑定账户 A,钱包当前账户为 B
- 步骤:
  1. 触发登录检测到账户不一致
- 预期结果:弹出"钱包账户不一致"对话框,展示应用账户与钱包账户,提供"切换/使用该钱包/取消"选项

### CH-014 已登录访问 /auth 自动跳回目标页
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已完成登录
- 步骤:
  1. 手动访问 `/#/auth?redirect=%2Fsettings`
- 预期结果:立即 `replace` 跳转到 `redirect` 解析目标(如 `/settings`);非法/为 `/auth` 的 redirect 回退到首页

### CH-015 退出登录清除本地授权会话
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录,进入设置页
- 步骤:
  1. 点击"退出登录"(`Settings.Account` 退出项)并确认
  2. 观察路由与本地存储
- 预期结果:断开钱包账号、清除本地 UCAN 授权会话;重新变为未登录态并被引导回 `/auth`

---

## 三、首页导航与路由

### CH-016 导航入口存在且可点击
- 优先级:P0
- 类型:UI
- 状态:✅ 已实现 — products/chat/tests/user-flow.spec.ts
- 前置条件:前端 3020 可用
- 步骤:
  1. 访问首页
  2. 统计 `a[href]/[role=link]/[role=menuitem]` 数量
- 预期结果:至少存在一个导航入口(DOM 可达)

### CH-017 侧栏渲染发现/新建会话/帮助/设置入口
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,首页
- 步骤:
  1. 观察左侧 SideBar
- 预期结果:渲染"发现(Discovery)""新建会话(NewChat)"主按钮,以及顶部帮助、设置入口与当前账号信息

### CH-018 点击新建会话进入 /new-chat
- 优先级:P0
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 点击侧栏"新建会话"
- 预期结果:导航到 `/#/new-chat`,展示技能选择与输入区,仍在 SPA 内无整页刷新

### CH-019 首页在无会话时自动进入新建会话
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录,已就绪文本/图像模型,且无任何含消息的会话
- 步骤:
  1. 访问首页 `/`
- 预期结果:自动 `replace` 到 `/#/new-chat`(引导用户开启第一段对话)

### CH-020 无可用模型时引导到 Setup 页
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录,但无可用文本/图像模型(无令牌/模型加载为空)
- 步骤:
  1. 访问 `/#/chat` 或首页
- 预期结果:被重定向到 `/#/setup?redirect=<原路径>`,引导选择/配置模型与令牌

### CH-021 模型加载失败展示重试入口
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,Router `llm.models()` 返回失败
- 步骤:
  1. 进入应用触发模型引导加载
- 预期结果:展示 Bootstrap 错误态与"重试"按钮,点击后重新拉取模型

### CH-022 帮助页 /help 免登录可访问
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:未登录
- 步骤:
  1. 访问 `/#/help`
- 预期结果:渲染帮助页(Chat 使用指南),无需登录即可查看

---

## 四、新建对话(new-chat)

### CH-023 空白会话:输入草稿后开始对话
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录,已就绪文本模型
- 步骤:
  1. 进入 `/#/new-chat`
  2. 在输入框填入草稿文本
  3. 点击空白会话卡片(`NewChat.BlankTitle`)开始
- 预期结果:以默认技能创建会话并携带草稿进入 `/#/chat`

### CH-024 新建会话模型下拉选择
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,存在多个可用模型
- 步骤:
  1. 打开 new-chat 模型下拉(默认占位 `NewChat.ModelPlaceholder`)
  2. 选择一个模型
- 预期结果:后续开始的会话使用所选模型

### CH-025 精选技能列表渲染
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,已加载技能
- 步骤:
  1. 进入 new-chat,观察"精选(FeaturedTitle)"区
- 预期结果:渲染若干精选技能卡片;无技能时显示空态与"探索技能"入口

### CH-026 点击精选技能创建对应会话
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录,存在精选技能
- 步骤:
  1. 点击某精选技能卡片
- 预期结果:以该技能预设创建会话并进入 `/#/chat`,技能上下文注入会话

### CH-027 "更多/探索技能"跳转发现页
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 点击 new-chat 的"更多(More)"或"探索技能"
- 预期结果:导航到 `/#/discovery?type=skill`

### CH-028 删除技能卡片
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:存在可删除的自定义技能
- 步骤:
  1. 在技能卡片上点击删除(`删除 <技能名>`)并确认
- 预期结果:该技能从列表移除并持久化

### CH-029 返回按钮回到首页
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:处于 new-chat
- 步骤:
  1. 点击返回(`NewChat.Return`)
- 预期结果:导航回首页 `/`

---

## 五、LLM 会话(依赖 key/令牌)

### CH-030 发送消息获得流式回复
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录且有可用文本模型;`OPENAI_API_KEY`/`ANTHROPIC_API_KEY` 或有效 Router 令牌可用
- 步骤:
  1. 在会话输入框输入问题
  2. 点击"发送"(`Chat.Send`)
- 预期结果:助手气泡以流式(SSE)逐字追加内容,最终形成完整回复

### CH-031 多轮上下文连续对话
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:同 CH-030
- 步骤:
  1. 发送第一条消息并获得回复
  2. 追加与上文相关的第二条消息
- 预期结果:第二次回复体现对上一轮上下文的记忆,历史消息完整保留

### CH-032 流式过程中"停止响应"中断
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:同 CH-030,消息正在流式输出
- 步骤:
  1. 回复流式输出中点击"停止"(`Chat.InputActions.Stop`)
- 预期结果:请求被中止,停止追加;已生成的部分内容保留,`ChatControllerPool` 无挂起请求

### CH-033 重试/重新生成回复
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已有一条助手回复
- 步骤:
  1. 对某条消息点击"重试(Retry)"
- 预期结果:基于同一上文重新发起请求并生成新回复

### CH-034 编辑已发送消息并重发
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已有用户消息
- 步骤:
  1. 对用户消息执行编辑(`Chat.Actions.Edit`)修改内容
  2. 重新发送
- 预期结果:消息内容更新,基于新内容重新生成后续回复

### CH-035 删除单条消息
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:会话含多条消息
- 步骤:
  1. 对某条消息点击"删除(Delete)"
- 预期结果:该消息从会话移除并持久化

### CH-036 复制消息内容
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已有助手回复
- 步骤:
  1. 点击消息"复制(Copy)"
- 预期结果:消息文本写入剪贴板

### CH-037 会话内切换模型
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:存在多个可用模型
- 步骤:
  1. 点击输入区模型选择(`Chat.InputActions` 模型)
  2. 选择另一模型
- 预期结果:后续消息使用新模型,当前会话模型标识更新

### CH-038 Markdown/代码块渲染
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:助手返回含 Markdown/代码的内容
- 步骤:
  1. 观察助手气泡渲染
- 预期结果:标题/列表/代码高亮正确渲染,代码块提供复制入口

### CH-039 上传图片进行多模态提问
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:当前模型在 `VISION_MODELS`(支持视觉)
- 步骤:
  1. 点击"上传图片(UploadImage)"选择图片
  2. 附带提问发送
- 预期结果:图片作为多模态输入随消息发送,模型给出基于图片的回复

### CH-040 无效/错误 API Key 时优雅报错
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:配置一个无效的 provider key(或无有效令牌)
- 步骤:
  1. 发送一条消息
- 预期结果:上游返回鉴权错误,会话内展示可读错误提示,页面不崩溃,可重试

### CH-041 清除当前会话上下文
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:会话已有多轮消息
- 步骤:
  1. 点击"清除上下文(Clear)"
- 预期结果:插入清除分隔标记,后续请求不再携带分隔线以上历史

### CH-042 历史过长自动摘要压缩
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:会话历史超过压缩阈值,`SUMMARIZE_MODEL` 可用
- 步骤:
  1. 持续对话使历史累积
  2. 触发发送
- 预期结果:超阈值历史被自动压缩为摘要作为上下文发送,回复仍连贯

---

## 六、会话管理

### CH-043 会话列表展示与切换
- 优先级:P0
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,存在多个会话
- 步骤:
  1. 观察侧栏会话列表
  2. 点击不同会话
- 预期结果:列表展示各会话标题与"共 N 条对话"副标题;点击原地切换到对应会话

### CH-044 新建并删除会话
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 新建一个会话
  2. 对其执行删除
- 预期结果:会话创建后出现在列表;删除后从列表移除(必要时提供撤销)

### CH-045 修改会话标题
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:存在会话
- 步骤:
  1. 编辑会话主题(`Chat.EditMessage`/标题编辑)修改标题
- 预期结果:标题更新并持久化

### CH-046 刷新/自动生成标题
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:`AutoGenerateTitle` 开启,会话已有内容
- 步骤:
  1. 点击"刷新标题(RefreshTitle)"或依赖自动生成
- 预期结果:根据对话内容生成新的会话标题

### CH-047 会话置顶/取消置顶
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:存在多个会话
- 步骤:
  1. 对某会话点击置顶(Pin)
- 预期结果:该会话固定到列表顶部;再次点击取消置顶

### CH-048 搜索会话 /search-chat
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,存在含关键词的历史消息
- 步骤:
  1. 进入 `/#/search-chat`
  2. 输入关键词搜索
- 预期结果:返回命中该关键词的会话/消息,点击可跳转定位

### CH-049 会话持久化(刷新后恢复)
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录并产生若干会话
- 步骤:
  1. 刷新页面
- 预期结果:会话列表与消息从本地存储(`chat-next-web-store`)恢复,不丢失

### CH-050 会话数据导入/合并
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:存在可导入的会话备份数据
- 步骤:
  1. 在设置中导入会话数据
- 预期结果:导入会话与本地会话合并,无重复/覆盖异常

---

## 七、Provider 代理 API

### CH-051 OpenAI 兼容对话代理转发
- 优先级:P0
- 类型:API
- 状态:⬜ 待实现
- 前置条件:`OPENAI_API_KEY`(或 `BASE_URL` 兼容网关)已配置
- 步骤:
  1. `POST /api/openai/v1/chat/completions` 携带消息体与合法访问凭证
- 预期结果:请求被注入系统 Key 后转发上游,返回上游回复(可流式)

### CH-052 Anthropic 对话代理转发
- 优先级:P0
- 类型:API
- 状态:⬜ 待实现
- 前置条件:`ANTHROPIC_API_KEY` 已配置
- 步骤:
  1. `POST /api/anthropic/v1/messages` 携带消息体
- 预期结果:注入 Anthropic 系统 Key 后转发,返回上游回复

### CH-053 未知 provider 返回 404
- 优先级:P1
- 类型:API
- 状态:⬜ 待实现
- 前置条件:无
- 步骤:
  1. `POST /api/unknownvendor/v1/chat`
- 预期结果:返回 404,JSON `{error:"Unknown provider: unknownvendor"}`

### CH-054 需要访问码时缺码被拒
- 优先级:P0
- 类型:API
- 状态:⬜ 待实现
- 前置条件:配置 `CODE`(needCode 为真),未随请求提供合法访问码或用户 Key
- 步骤:
  1. `POST /api/openai/v1/chat/completions` 不带访问码/Key
- 预期结果:被拦截,返回错误 `empty access code`(或提供错误码时 `wrong access code`)

### CH-055 携带正确访问码放行
- 优先级:P1
- 类型:API
- 状态:⬜ 待实现
- 前置条件:配置 `CODE`,已知一个合法访问码
- 步骤:
  1. `Authorization: Bearer nk-<code>` 发起对话请求
- 预期结果:访问码校验通过(md5 命中白名单),注入系统 Key 并转发

### CH-056 HIDE_USER_API_KEY 时拒绝自带 Key
- 优先级:P1
- 类型:API
- 状态:⬜ 待实现
- 前置条件:`HIDE_USER_API_KEY=1`
- 步骤:
  1. 用非访问码前缀的用户自带 Key 作 `Authorization` 请求对话
- 预期结果:被拒,返回 `you are not allowed to access with your own api key`

### CH-057 未配置系统 Key 时的降级
- 优先级:P2
- 类型:API
- 状态:⬜ 待实现
- 前置条件:对应 provider 系统 Key 为空,且未提供用户 Key
- 步骤:
  1. 发起该 provider 的对话请求
- 预期结果:不注入 Key(日志 `system api key is not configured`),上游因缺 Key 返回鉴权错误,前端可读展示

### CH-058 GET /api/openai/v1/models 列出模型
- 优先级:P2
- 类型:API
- 状态:⬜ 待实现
- 前置条件:OpenAI 兼容上游可用且已配置 Key
- 步骤:
  1. `GET /api/openai/v1/models` 携带合法凭证
- 预期结果:透传上游模型列表(用于前端模型选择)

---

## 八、设置(Settings)

### CH-059 打开设置页展示分组
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 进入 `/#/settings`
- 预期结果:渲染设置分组(账号/外观/同步/危险操作等),标题 `设置`

### CH-060 展示区块链地址(当前账户)
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 查看账号区"区块链地址"
- 预期结果:展示当前登录钱包地址

### CH-061 切换主题(深色/浅色/自动)
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 切换主题设置
- 预期结果:`body` 类切换为 `dark`/`light`,`theme-color` meta 相应更新并持久化

### CH-062 配置访问码/自定义模型
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 在设置中填写访问码/自定义模型名
- 预期结果:配置保存;后续请求携带访问码,自定义模型进入模型列表

### CH-063 注入系统级提示与用户输入预处理模板
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 开启"注入系统级提示信息"
  2. 编辑"用户输入预处理"模板
- 预期结果:开启后每次请求消息列表开头追加系统提示;用户最新消息按模板预处理后发送

### CH-064 重置所有设置
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已修改若干设置项
- 步骤:
  1. 点击"重置所有设置"并确认(`Danger.Reset`)
- 预期结果:所有设置项恢复默认值

### CH-065 清除所有数据
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:存在会话与设置数据
- 步骤:
  1. 点击"清除所有数据"并确认(`Danger.Clear`)
- 预期结果:清空所有聊天与设置数据,应用回到初始态

---

## 九、云同步与 WebDAV 代理

### CH-066 存储页展示云同步状态
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,进入 `/#/storage`
- 步骤:
  1. 观察存储/同步状态区
- 预期结果:根据配置展示"需配置/检查中/错误/就绪"状态与配额信息

### CH-067 检查连接(WebDAV 可达性)
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:配置 `WEBDAV_BACKEND_BASE_URL`(如本地 Warehouse 6065)
- 步骤:
  1. 在存储页点击"检查连接"
- 预期结果:连接成功显示就绪并可拉取配额;失败显示错误态

### CH-068 立即同步会话数据
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:云同步已配置且连接就绪
- 步骤:
  1. 点击"立即同步"
  2. 刷新后观察会话列表
- 预期结果:本地会话上传/下行合并成功,`lastSyncTime` 更新;刷新后会话可从云端恢复

### CH-069 WebDAV 代理白名单校验
- 优先级:P1
- 类型:API
- 状态:⬜ 待实现
- 前置条件:`ALLOW_LOCAL_WEBDAV=0`(生产)
- 步骤:
  1. 通过 `/api/webdav/<path>?endpoint=<非白名单地址>` 代理请求
- 预期结果:非白名单/本地地址被拒绝转发;仅内置白名单与已配置后端地址放行

### CH-070 WebDAV 代理透传 PROPFIND/PUT/GET
- 优先级:P1
- 类型:API
- 状态:⬜ 待实现
- 前置条件:后端 WebDAV 可用,`endpoint` 指向合法后端
- 步骤:
  1. 经 `/api/webdav/...` 以 `proxy_method` 发起 PROPFIND/PUT/GET
- 预期结果:代理按方法转发并回传后端响应(207/2xx),用于浏览器绕过跨域

### CH-071 自动同步开关与间隔
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:云同步已配置
- 步骤:
  1. 开启"自动同步"并设置间隔(分钟)
- 预期结果:按间隔自动触发同步(`useAutoSync`),状态区反映最近同步时间

---

## 十、Router 令牌与充值

### CH-072 Router 页展示可用令牌
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,`ROUTER_BACKEND_URL` 可达
- 步骤:
  1. 进入 `/#/router`
- 预期结果:列出可选公共令牌(脱敏显示前缀/后缀)与令牌可用模型能力标签(文本/视觉/图像/推理等)

### CH-073 选择令牌用于会话
- 优先级:P0
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:存在可选 Router 令牌
- 步骤:
  1. 在 Router 页选择一个令牌
  2. 返回发起对话
- 预期结果:`selectedRouterToken` 写入,模型基于该令牌加载,会话通过 Router 转发成功

### CH-074 自动挑选可用公共令牌(引导)
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录但未手动选令牌、未配置用户 Key
- 步骤:
  1. 进入应用触发引导加载
- 预期结果:`ensureRouterToken` 自动选取一个可用公共令牌写入,模型加载就绪

### CH-075 令牌用量/余额状态展示
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已选令牌或配置用户 Key
- 步骤:
  1. 查看 Router 页用量/状态区
- 预期结果:展示令牌用量/到期状态;未设置时显示对应占位

### CH-076 充值/续费跳转管理中心
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:配置 `ROUTER_PORTAL_URL`/充值页地址
- 步骤:
  1. 点击充值/续费引导入口
- 预期结果:跳转到 Router 管理中心对应页(令牌页/充值页地址按配置继承回退)

---

## 十一、技能/面具/插件/工具/发现

### CH-077 发现页浏览技能/工具
- 优先级:P1
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,进入 `/#/discovery`
- 步骤:
  1. 按 `type=skill`/`type=tool` 浏览列表
- 预期结果:渲染 marketplace 技能/工具条目,展示可配置/不可用等状态

### CH-078 技能编辑器创建自定义技能
- 优先级:P1
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:已登录,进入 `/#/skills`
- 步骤:
  1. 新建技能,填写名称/预设提示等并保存
- 预期结果:技能保存到 `skill-store`,出现在技能列表并可用于新建会话

### CH-079 面具路由 /masks 兼容旧入口
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 访问 `/#/masks`
- 预期结果:复用技能编辑页(Masks 为兼容路由),功能等价于 skills

### CH-080 插件页 /plugins 管理
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 进入 `/#/plugins`
- 预期结果:展示插件列表与启用/配置入口

### CH-081 工具市场 /tools 浏览
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 进入 `/#/tools`
- 预期结果:展示可用工具(marketplace),可查看详情/配置

### CH-082 启用工具运行时(ENABLE_TOOLS)
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:`ENABLE_TOOLS=1`,standalone 运行且可读写 `data/tool_config.json`
- 步骤:
  1. 启用某工具并在会话中触发调用
- 预期结果:工具系统初始化成功(`initializeToolSystem`),工具经 MCP 被调用并返回结果

### CH-083 工具未启用时降级
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:`ENABLE_TOOLS=0` 或桌面静态导出
- 步骤:
  1. 查看工具相关入口
- 预期结果:工具运行时不初始化,相关能力优雅隐藏/禁用,不报错

---

## 十二、图像生成(Sd)

### CH-084 进入图像生成页 /sd
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录,存在图像能力模型或 Stability 配置
- 步骤:
  1. 访问 `/#/sd`
- 预期结果:渲染图像生成界面(尺寸/质量/风格等参数选择)

### CH-085 提交提示词生成图片
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:`STABILITY_API_KEY` 或有图像能力的 Router 令牌可用
- 步骤:
  1. 输入提示词,选择参数并提交
- 预期结果:调用图像生成端点,返回图片并加入 `sd-list` 展示

### CH-086 /sd-new 重定向到 /sd
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已登录
- 步骤:
  1. 访问 `/#/sd-new`
- 预期结果:`Navigate` 重定向到 `/#/sd`

### CH-087 图像生成历史列表
- 优先级:P2
- 类型:UI
- 状态:⬜ 待实现
- 前置条件:已生成过图片
- 步骤:
  1. 查看 Sd 历史列表
- 预期结果:列出历史生成记录(`sd-list`),可查看/删除

---

## 十三、导出与分享

### CH-088 导出会话为 Markdown
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:会话含消息
- 步骤:
  1. 打开"分享聊天记录",选择导出格式 Markdown 并导出
- 预期结果:生成 Markdown 文本(可含/不含技能上下文,按开关)

### CH-089 导出会话为 PNG 图片
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:会话含消息
- 步骤:
  1. 在导出对话框选择 PNG 格式导出
- 预期结果:生成会话截图 PNG

### CH-090 通过 artifacts 分享生成分享页
- 优先级:P1
- 类型:API
- 状态:⬜ 待实现
- 前置条件:已配置 Cloudflare KV(账号/命名空间/API Key)
- 步骤:
  1. `POST /api/artifacts` 提交内容
- 预期结果:返回 `{code:0, id:<hash>}`,内容写入 KV;未配置 KV 时返回错误 `Save data error`

### CH-091 读取 artifacts 分享内容
- 优先级:P1
- 类型:API
- 状态:⬜ 待实现
- 前置条件:已存在分享 id
- 步骤:
  1. `GET /api/artifacts?id=<id>`
- 预期结果:返回该分享的原始内容,供 `/#/artifacts/:id` 免登录渲染

### CH-092 ShareGPT 分享导出
- 优先级:P2
- 类型:E2E
- 状态:⬜ 待实现
- 前置条件:会话含消息
- 步骤:
  1. 触发 ShareGPT 分享
- 预期结果:生成 ShareGPT 分享链接/内容,可外部访问
